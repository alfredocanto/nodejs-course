export function add(x, y) {
  return x + y
}

function substact(x, y) {
  return x - y
}

function multiply(x, y) {
  return x * y
}

function divide(x, y) {
  return x / y
}

export default {
  add,
  substact,
  multiply,
  divide,
}
